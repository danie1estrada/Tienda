package com.example.tienda.providers.utils.interfaces;

import com.example.tienda.framework.database.room.providers.entities.Provider;

public interface OnProviderSelected {
    void onProviderSelected(Provider provider);
}
